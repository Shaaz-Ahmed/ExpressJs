const express = require("express");
const app = express();

app.get("/", (req,res)=>{
    res.send("Hello world welcome to express js framework of nodejs");
});

app.listen(9000, ()=>{
    console.log("Listening to port no 9000");
});
